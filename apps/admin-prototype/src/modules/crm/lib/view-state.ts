/**
 * Re-exported from `@/lib/view-state` — this was CRM-local before it was
 * promoted to a shared primitive (nothing in it was actually CRM-specific).
 * Kept so existing imports in this module don't need touching; new code
 * should import from `@/lib/view-state` directly.
 */
export * from '@/lib/view-state'
